// Place in the city

model Place {
    id   Int    @id @default(autoincrement())
    name String

    cityId Int
    city   City @relation(fields: [cityId], references: [id])

    User User[]

    @@map("places")
}
